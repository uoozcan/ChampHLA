# Benchmark Analysis - 2026-04-01

## Status

The synthetic fixture benchmark is now retained only for CI and parser validation. It is no longer the source of scientific claims, manuscript numbers, or benchmark-facing interpretation.

Real analyses should now be generated from the 1000 Genomes real-data workflow:
- `bin/build_1000g_benchmark_manifests.py`
- `bin/run_1000g_benchmark.py`
- `conf/benchmark_1000g_phase_gated_abc.yaml`

## Current Run State (updated 2026-04-18)

All modality-specific benchmark runs have completed successfully on Puhti:
- WGS 44-sample benchmark: completed 2026-04-16 → `analysis/benchmark_wgs_44samples/`
- WES 50-sample benchmark: completed 2026-04-17 → `analysis/benchmark_wes_50samples/`
- RNA 50-sample benchmark: completed 2026-04-17 → `analysis/benchmark_rna_50samples/`
- Full tri-modal cohort benchmark (WES=12, WGS=6, RNA=7 holdout): completed 2026-04-15 → `analysis/full_cohort_benchmark/`
- Full tri-modal 50-sample benchmark (WES=12, WGS=20, RNA=7 holdout, 100-sample cohort): completed 2026-04-18 → `analysis/benchmark_trimodal_50samples/`
  - 515 harmonized rows with confidence ingested
  - Figure generation submitted: SLURM job 34063520 → `analysis/figures_final/`

## Benchmark Source Registry

This table maps each set of manuscript numbers to the authoritative runtime benchmark directory
under `/scratch/project_2008084/pihla-publish/analysis/`. Cross-verified 2026-04-18.

| Modality / Result set | Benchmark directory | Holdout n | Key numbers verified |
|---|---|---|---|
| WGS single-tool + consensus | `1000g_realdata/benchmark_wgs_wave1/` | 9 | OptiType 0.5185, WC callable 0.9259, ArcasHLA 0.0, MajorityVote 0.4815 ✓ |
| WGS ArcasHLA guardrail | `1000g_realdata/benchmark_wgs_wave1/` | — | guardrail_factor 0.9867 = 98.7% shrinkage ✓ |
| WGS abstention tradeoff | `1000g_realdata/benchmark_wgs_wave1/` | 9 | min_support 0.45 → callable 0.9259, accuracy 0.560 ✓ |
| WES single-tool + consensus | `full_cohort_benchmark/` | 12 | OptiType 0.9167, MajorityVote 0.9444, WC 0.8611 ✓ |
| RNA single-tool + consensus | `full_cohort_benchmark/` | 7 | ArcasHLA/HLA-HD/OptiType 1.000 ✓ |
| Confidence weights (WGS) | `1000g_realdata/benchmark_wgs_wave1/tables/tool_confidence_weights.tsv` | — | HLA-HD/Kourami/OptiType poor_calibration; T1K applied ✓ |
| Tri-modal cross-modality comparison | `benchmark_trimodal_50samples/` | WES=12, WGS=20, RNA=7 | See WGS discrepancy note below |

**Note**: The WGS primary reporting source is `benchmark_wgs_wave1` (42 WGS samples, 25/8/9 split).
The `full_cohort_benchmark` is the source for WES and RNA holdout numbers.
These are separate benchmark runs with different cohort compositions.

## WGS Accuracy Discrepancy — benchmark_wgs_wave1 vs benchmark_trimodal_50samples

**Do not compare WGS numbers directly between these two benchmarks.**

| Metric | benchmark_wgs_wave1 | benchmark_trimodal_50samples |
|---|---|---|
| WeightedConsensus overall accuracy | **0.9259** | **0.2667** |
| WeightedConsensus callable rate | 0.9259 | 0.5667 |
| MajorityVote overall accuracy | 0.4815 | 0.2333 |
| Holdout n | 9 | 20 |

**Root cause**: The two benchmarks use different underlying WGS data:
- `benchmark_wgs_wave1`: WGS results from a single curated batch (`hla_calibration/wgs/`), POLYSOLVER included, high per-tool accuracy (OptiType 0.519).
- `benchmark_trimodal_50samples`: WGS results from multi-batch processing (`hla_calibration/wgs_batches/*/`), POLYSOLVER not available for WGS, individual tool accuracy 8–43%, most tools fire `poor_calibration` guardrail.

The lower callable rate in trimodal (0.567) reflects fewer tools producing concordant calls when POLYSOLVER is absent and when tool accuracy is lower. The ensemble cannot rescue low-quality individual calls.

**Manuscript guidance**: Cite `benchmark_wgs_wave1` for WGS performance claims. Use `benchmark_trimodal_50samples` only for cross-modality comparison figures showing relative performance of WES vs RNA vs WGS within the same 100-sample cohort. Do not present the trimodal WGS numbers (0.267) as the primary WGS accuracy claim.

## Scientific Benchmark Policy

All scientific summaries must use only 1000 Genomes samples that satisfy all of the following:
- public HLA ground truth from the Gourraud et al. dataset
- matched `wgs`, `wes`, and `rnaseq` availability
- truth-supported benchmark loci only

Current headline truth-supported loci:
- `A`
- `B`
- `C`

Detailed tool result files may still contain additional loci, but they are outside the current headline benchmark scope.

Primary endpoint:
- exact two-field allele-pair match

Secondary endpoint:
- exact three-field allele-pair match where derivable and comparable

## Required Output Tables For Real Analyses

- `truth_manifest.tsv`
- `sequencing_manifest.tsv`
- `cohort_manifest.tsv`
- `harmonized_benchmark_rows.tsv`
- `summary_full_cohort.tsv`
- `summary_per_gene.tsv`
- `ambiguity_summary.tsv`
- `ambiguity_summary_by_gene.tsv`
- `majority_vote_baseline.tsv`
- `weighted_consensus_calls.tsv`
- `method_comparison.tsv`
- `method_ambiguity_summary.tsv`
- `method_ambiguity_summary_by_gene.tsv`
- `confidence_calibration_summary.tsv`
- `confidence_error_summary.tsv`
- `confidence_error_summary_by_gene.tsv`
- `abstention_tradeoff.tsv`
- `discordance_summary.tsv`
- `benchmark_metadata.json`

## Interpretation Guidance

When a refreshed real 1000 Genomes run is available, the main interpretation order should be:
1. cohort assembly and tri-modal filtering
2. single-tool performance by modality and gene
3. majority vote versus weighted consensus on holdout
4. calibration and confidence-stratified error analysis
5. ambiguity-aware evaluation at 2-field and 3-field
6. abstention behavior
7. discordance patterns across DNA and RNA modalities

## Phase-Gated Reporting Policy

If a tool fails in a modality during the current refresh:
- report it explicitly as `unavailable` in metadata and coverage summaries,
- exclude it from method-comparison numerators for that modality,
- preserve already completed modalities for that tool when valid,
- do not convert engineering failure into a biological performance claim,
- keep headline narrative focused on the valid truth-backed comparison set.

This policy is especially important while the Puhti execution layer is still being hardened for storage-heavy WES and RNA runs.

## Notes

The fixture-based development benchmark remains useful for:
- regression testing benchmark tables
- parser validation
- confidence and consensus plumbing checks

It should not be cited as a real cohort analysis in scientific text.
